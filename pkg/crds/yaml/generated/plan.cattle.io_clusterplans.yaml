---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: clusterplans.plan.cattle.io
spec:
  group: plan.cattle.io
  names:
    kind: ClusterPlan
    listKind: ClusterPlanList
    plural: clusterplans
    shortNames:
    - cp
    singular: clusterplan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.operation
      name: Operation
      type: string
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.currentStep
      name: Step
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterPlan is the concrete rendered plan for one invocation of a day-2
          operation against one cluster. ClusterPlans live in the cluster's
          namespace: fleet-default for provisioning.cattle.io/v1.Cluster (CAPR /
          CAPRKE2), the management namespace c-XXXXX for imported clusters whose
          v3.Cluster is cluster-scoped.

          Spec.Plan holds an ordered list of NodePoolPlan entries — each pool
          targets a node set (via Selector) or a single elected node (via Election),
          inlines a NodePlanSpec describing what to do, and may carry before/after
          patches against arbitrary resources. Cluster-wide Files / Probes /
          Instructions (each carrying their own selectors) sit at the top level
          alongside Plan and are layered onto every matching node before pool
          execution.

          The trigger controller materialises a ClusterPlan and writes the Beacon
          acquisition request. Once the Beacon is held, the renderer fills Spec.Plan
          from Spec.TemplateRef. The stage controller drives execution and emits
          per-node NodePlan resources from each pool entry; system-agents discover
          their NodePlan via the Beacon's ActiveSelectors and the registration
          endpoint.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterPlanSpec is the desired state of a ClusterPlan.
            properties:
              clusterRef:
                description: |-
                  ClusterRef points at the parent cluster object. Always interpreted via
                  the cluster-type adapter:
                    capr/caprke2 → provisioning.cattle.io/v1.Cluster
                    imported     → management.cattle.io/v3.Cluster
                properties:
                  apiVersion:
                    description: APIVersion of the cluster object.
                    minLength: 1
                    type: string
                  kind:
                    description: Kind of the cluster object (e.g. "Cluster").
                    minLength: 1
                    type: string
                  name:
                    description: Name of the cluster object.
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace of the cluster object. Empty for cluster-scoped clusters
                      (the imported management.cattle.io/v3.Cluster case).
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              entrypointRef:
                description: |-
                  EntrypointRef pins which entrypoint object was used for rendering.
                  Useful when a template lists multiple entrypoints (e.g. "works on
                  either RKEControlPlane or RKE2ControlPlane").
                properties:
                  apiVersion:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - apiVersion
                - kind
                type: object
              files:
                description: |-
                  Files are cluster-wide files layered onto every matching node's
                  rendered NodePlan, keyed by per-entry selector.
                items:
                  description: NodePoolFile is a cluster-wide file layered onto every
                    matching node.
                  properties:
                    content:
                      description: |-
                        Content of the file. May be base64-encoded by the agent transport for
                        binary safety; templating in the calling layer takes care of encoding.
                      type: string
                    drain:
                      description: |-
                        Drain=true causes the system-agent to drain the node before applying
                        the file change. Useful for kubelet config, container runtime config,
                        and other host-level mutations whose visibility requires a restart.
                      type: boolean
                    path:
                      description: Path is the absolute filesystem path on the node.
                      minLength: 1
                      type: string
                    permissions:
                      description: Permissions in octal string form, e.g. "0644".
                      type: string
                    selector:
                      description: |-
                        Selector selects which nodes receive the file. An empty selector
                        matches all nodes.
                      items:
                        description: |-
                          A label selector is a label query over a set of resources. The result of matchLabels and
                          matchExpressions are ANDed. An empty label selector matches all objects. A null
                          label selector matches no objects.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                      type: array
                  type: object
                type: array
              inputs:
                additionalProperties:
                  type: string
                description: |-
                  Inputs carries free-form parameters supplied at trigger time
                  (e.g. snapshotName, S3 bucket override). Available to templates as
                  `.Inputs`.
                type: object
              instructions:
                description: |-
                  Instructions are cluster-wide instructions layered onto every
                  matching node's rendered NodePlan.
                items:
                  description: |-
                    NodePoolInstruction is a cluster-wide instruction layered onto every
                    matching node.
                  properties:
                    args:
                      description: Args are arguments passed to Command.
                      items:
                        type: string
                      type: array
                    command:
                      description: Command is the executable to run.
                      minLength: 1
                      type: string
                    env:
                      description: Env is a list of "KEY=VALUE" strings.
                      items:
                        type: string
                      type: array
                    image:
                      description: |-
                        Image, when set, runs the command inside the named container image.
                        When empty, the command runs on the host.
                      type: string
                    name:
                      description: Name is a human-readable identifier referenced
                        by Output.Source.
                      minLength: 1
                      type: string
                    saveOutput:
                      description: |-
                        SaveOutput causes stdout to be retained by the agent so an Output
                        declaration can capture it. Will be removed once preserveStdout /
                        preserveStderr are wired through.
                      type: boolean
                    selector:
                      description: Selector selects which nodes run the instruction.
                      items:
                        description: |-
                          A label selector is a label query over a set of resources. The result of matchLabels and
                          matchExpressions are ANDed. An empty label selector matches all objects. A null
                          label selector matches no objects.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                      type: array
                    strategy:
                      description: Strategy controls retries, idempotency, and scheduling.
                      properties:
                        failureThreshold:
                          description: |-
                            FailureThreshold is the number of consecutive failures after which
                            the parent NodePlan is marked Failed.
                          type: integer
                        periodSeconds:
                          description: |-
                            PeriodSeconds, when > 0, runs the instruction on a recurring
                            schedule rather than once.
                          type: integer
                        retryOnStartup:
                          description: |-
                            RetryOnStartup re-runs the instruction every time the system-agent
                            process starts.
                          type: boolean
                        retryOnSuccess:
                          description: |-
                            Idempotent indicates the instruction may be safely re-run; when
                            false, an agent restart that interrupts the instruction will mark
                            the plan Failed rather than re-running.
                          type: boolean
                        successThreshold:
                          description: |-
                            SuccessThreshold is the number of consecutive successful runs
                            required before the instruction is considered done.
                          type: integer
                        timeoutSeconds:
                          description: TimeoutSeconds bounds a single execution attempt.
                          type: integer
                      type: object
                  type: object
                type: array
              lifecycle:
                description: |-
                  Lifecycle is copied from the template's lifecycle so the Beacon
                  controller does not need to fetch the template to evaluate cancellation.
                properties:
                  cancellable:
                    default: false
                    description: |-
                      Cancellable=true means an incoming operation marked CancelsOthers may
                      pre-empt this one while it holds the Beacon.
                    type: boolean
                  cancelsOthers:
                    default: false
                    description: |-
                      CancelsOthers=true means rendering this operation may pre-empt a
                      Cancellable holder of the Beacon.
                    type: boolean
                  timeoutSeconds:
                    default: 3600
                    description: |-
                      TimeoutSeconds is the maximum wall-clock time the operation may hold
                      the Beacon. If exceeded, the framework forcibly clears the Beacon and
                      marks the operation Failed.
                    format: int32
                    minimum: 60
                    type: integer
                type: object
              operation:
                description: |-
                  Operation is copied from the template for indexed lookup and for
                  human inspection in `kubectl get clusterplans -o wide`.
                minLength: 1
                type: string
              plan:
                description: |-
                  Plan is the ordered list of node-pool plans the framework executes
                  in sequence. Empty until the renderer fills it after Beacon
                  acquisition (or when authored directly).
                items:
                  description: |-
                    NodePoolPlan is one ordered step in a ClusterPlan: a target node set
                    plus the NodePlanSpec to apply, plus optional before/after patches
                    against arbitrary k8s resources.
                  properties:
                    concurrency:
                      anyOf:
                      - type: integer
                      - type: string
                      description: |-
                        Concurrency limits per-node parallelism in this pool. Encoded as
                        IntOrString so callers can say `"all"` or an integer. Defaults to
                        1 when nil/empty.
                      x-kubernetes-int-or-string: true
                    election:
                      description: |-
                        Election elects a single node from a candidate pool, applies a
                        label, and re-evaluates eligibility on every reconcile so that
                        drift triggers re-election. Mutually exclusive with Selector.
                      properties:
                        criteria:
                          description: |-
                            Criteria are templated boolean expressions (Go text/template). All
                            criteria must render to "true" for a node to remain elected. When
                            any criterion drifts to false, the label is removed and a new
                            election runs over the remaining candidates.
                          items:
                            type: string
                          type: array
                        selector:
                          description: |-
                            Selector restricts the candidate pool. Empty means any node in
                            the cluster is eligible.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        targetLabel:
                          description: |-
                            TargetLabel is applied to the elected node's "node-of-truth" object
                            (CAPI Machine for capr/caprke2, v3.Node for imported). The value
                            applied is "true".
                          minLength: 1
                          type: string
                      type: object
                    files:
                      description: Files are written to the node filesystem before
                        Instructions run.
                      items:
                        description: File is written by the system-agent to the node
                          filesystem.
                        properties:
                          content:
                            description: |-
                              Content of the file. May be base64-encoded by the agent transport for
                              binary safety; templating in the calling layer takes care of encoding.
                            type: string
                          drain:
                            description: |-
                              Drain=true causes the system-agent to drain the node before applying
                              the file change. Useful for kubelet config, container runtime config,
                              and other host-level mutations whose visibility requires a restart.
                            type: boolean
                          path:
                            description: Path is the absolute filesystem path on the
                              node.
                            minLength: 1
                            type: string
                          permissions:
                            description: Permissions in octal string form, e.g. "0644".
                            type: string
                        type: object
                      type: array
                    instructions:
                      description: Instructions are executed in order.
                      items:
                        description: Instruction is one shell command executed by
                          the system-agent.
                        properties:
                          args:
                            description: Args are arguments passed to Command.
                            items:
                              type: string
                            type: array
                          command:
                            description: Command is the executable to run.
                            minLength: 1
                            type: string
                          env:
                            description: Env is a list of "KEY=VALUE" strings.
                            items:
                              type: string
                            type: array
                          image:
                            description: |-
                              Image, when set, runs the command inside the named container image.
                              When empty, the command runs on the host.
                            type: string
                          name:
                            description: Name is a human-readable identifier referenced
                              by Output.Source.
                            minLength: 1
                            type: string
                          saveOutput:
                            description: |-
                              SaveOutput causes stdout to be retained by the agent so an Output
                              declaration can capture it. Will be removed once preserveStdout /
                              preserveStderr are wired through.
                            type: boolean
                          strategy:
                            description: Strategy controls retries, idempotency, and
                              scheduling.
                            properties:
                              failureThreshold:
                                description: |-
                                  FailureThreshold is the number of consecutive failures after which
                                  the parent NodePlan is marked Failed.
                                type: integer
                              periodSeconds:
                                description: |-
                                  PeriodSeconds, when > 0, runs the instruction on a recurring
                                  schedule rather than once.
                                type: integer
                              retryOnStartup:
                                description: |-
                                  RetryOnStartup re-runs the instruction every time the system-agent
                                  process starts.
                                type: boolean
                              retryOnSuccess:
                                description: |-
                                  Idempotent indicates the instruction may be safely re-run; when
                                  false, an agent restart that interrupts the instruction will mark
                                  the plan Failed rather than re-running.
                                type: boolean
                              successThreshold:
                                description: |-
                                  SuccessThreshold is the number of consecutive successful runs
                                  required before the instruction is considered done.
                                type: integer
                              timeoutSeconds:
                                description: TimeoutSeconds bounds a single execution
                                  attempt.
                                type: integer
                            type: object
                        type: object
                      type: array
                    name:
                      description: |-
                        Name is optional. When omitted, status references the pool by
                        integer index in Plan.
                      type: string
                    nodePlanTemplateRef:
                      description: |-
                        NodePlanTemplateRef is an optional reference to a NodePlanTemplate
                        from which the per-node NodePlan is rendered. When unset, the
                        pool's inlined NodePlanSpec is used directly.
                      properties:
                        name:
                          minLength: 1
                          type: string
                      required:
                      - name
                      type: object
                    outputs:
                      description: |-
                        Outputs declare which Instruction stdouts to capture and surface in
                        Status.Outputs. The capture itself is performed by the agent; the
                        framework consumes them via Status.
                      items:
                        description: Output declares a value to surface in NodePlanStatus.Outputs.
                        properties:
                          name:
                            description: Name is the key under which the captured
                              value is published.
                            minLength: 1
                            type: string
                          persistent:
                            description: |-
                              Persistent=true keeps the output across the lifetime of the
                              NodePlan and (when this NodePlan descends from a ClusterPlan)
                              promotes it into the parent ClusterPlan's persistent outputs map.
                            type: boolean
                          source:
                            description: |-
                              Source identifies the producer. The framework recognises the form
                              "<instructionName>" (capture instruction stdout) and
                              "<instructionName>:<jsonPath>" (capture and project as JSON).
                            minLength: 1
                            type: string
                        type: object
                      type: array
                    patches:
                      description: |-
                        Patches are applied around pool execution: Before runs prior to
                        emitting NodePlans, After runs once every selected NodePlan has
                        reached NodePlanPhaseSucceeded.
                      properties:
                        after:
                          description: After is applied once every selected NodePlan
                            has succeeded.
                          items:
                            description: Patch is a named bundle of one or more patch
                              definitions.
                            properties:
                              definitions:
                                description: Definitions to apply, in order.
                                items:
                                  description: PatchDefinition selects a target object
                                    and the JSON patches to apply.
                                  properties:
                                    jsonPatches:
                                      description: JSONPatches is the RFC 6902 op
                                        list (already templated).
                                      items:
                                        description: |-
                                          JSONPatch is one RFC 6902 op. Value is encoded as a string in this version
                                          — callers JSON-encode complex values themselves to avoid the OpenAPI
                                          schema friction of opaque embedded values. May be widened to a structured
                                          value type in a follow-up.
                                        properties:
                                          op:
                                            type: string
                                          path:
                                            type: string
                                          value:
                                            type: string
                                        type: object
                                      type: array
                                    selector:
                                      description: Selector identifies the target.
                                      properties:
                                        apiVersion:
                                          type: string
                                        kind:
                                          type: string
                                        name:
                                          type: string
                                        namespace:
                                          type: string
                                      type: object
                                  type: object
                                type: array
                              name:
                                description: Name is a human-readable identifier surfaced
                                  in status.
                                type: string
                            type: object
                          type: array
                        before:
                          description: Before is applied prior to emitting NodePlans
                            for the pool.
                          items:
                            description: Patch is a named bundle of one or more patch
                              definitions.
                            properties:
                              definitions:
                                description: Definitions to apply, in order.
                                items:
                                  description: PatchDefinition selects a target object
                                    and the JSON patches to apply.
                                  properties:
                                    jsonPatches:
                                      description: JSONPatches is the RFC 6902 op
                                        list (already templated).
                                      items:
                                        description: |-
                                          JSONPatch is one RFC 6902 op. Value is encoded as a string in this version
                                          — callers JSON-encode complex values themselves to avoid the OpenAPI
                                          schema friction of opaque embedded values. May be widened to a structured
                                          value type in a follow-up.
                                        properties:
                                          op:
                                            type: string
                                          path:
                                            type: string
                                          value:
                                            type: string
                                        type: object
                                      type: array
                                    selector:
                                      description: Selector identifies the target.
                                      properties:
                                        apiVersion:
                                          type: string
                                        kind:
                                          type: string
                                        name:
                                          type: string
                                        namespace:
                                          type: string
                                      type: object
                                  type: object
                                type: array
                              name:
                                description: Name is a human-readable identifier surfaced
                                  in status.
                                type: string
                            type: object
                          type: array
                      type: object
                    probes:
                      description: |-
                        Probes are evaluated periodically and gate Instruction execution
                        according to each instruction's strategy.
                      items:
                        description: Probe is a periodic readiness check evaluated
                          by the system-agent.
                        properties:
                          httpGetAction:
                            description: |-
                              HTTPGetAction defines the HTTP probe. Future kinds (TCPSocket,
                              Exec) may be added alongside.
                            properties:
                              caCert:
                                description: CACert is a PEM-encoded CA certificate
                                  (or a path on the node).
                                type: string
                              clientCert:
                                description: |-
                                  ClientCert is a PEM-encoded client certificate (or a path on the
                                  node, agent-dependent).
                                type: string
                              clientKey:
                                description: ClientKey is a PEM-encoded client key
                                  (or a path on the node).
                                type: string
                              insecure:
                                description: Insecure skips TLS verification.
                                type: boolean
                              url:
                                description: URL of the probe target.
                                minLength: 1
                                type: string
                            type: object
                          name:
                            description: |-
                              Name identifies the probe and is referenced by Output and by
                              instruction-status conditions.
                            minLength: 1
                            type: string
                          retryStrategy:
                            description: |-
                              RetryStrategy controls evaluation cadence and the success/failure
                              thresholds that flip the probe's reported status.
                            properties:
                              failureThreshold:
                                description: |-
                                  FailureThreshold is the consecutive failures required to flip the
                                  probe to NotReady.
                                type: integer
                              initialDelaySeconds:
                                description: InitialDelaySeconds delays the first
                                  evaluation.
                                type: integer
                              successThreshold:
                                description: |-
                                  SuccessThreshold is the consecutive successes required to flip
                                  the probe to Ready.
                                type: integer
                              timeoutSeconds:
                                description: TimeoutSeconds bounds a single probe
                                  attempt.
                                type: integer
                            type: object
                        type: object
                      type: array
                    selector:
                      description: |-
                        Selector selects nodes for this pool. Mutually exclusive with
                        Election. When both are nil, the pool is skipped (treated as
                        "no nodes match"); the framework records this in StageStatus.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
                type: array
              probes:
                description: |-
                  Probes are cluster-wide probes layered onto every matching node's
                  rendered NodePlan.
                items:
                  description: NodePoolProbe is a cluster-wide probe layered onto
                    every matching node.
                  properties:
                    httpGetAction:
                      description: |-
                        HTTPGetAction defines the HTTP probe. Future kinds (TCPSocket,
                        Exec) may be added alongside.
                      properties:
                        caCert:
                          description: CACert is a PEM-encoded CA certificate (or
                            a path on the node).
                          type: string
                        clientCert:
                          description: |-
                            ClientCert is a PEM-encoded client certificate (or a path on the
                            node, agent-dependent).
                          type: string
                        clientKey:
                          description: ClientKey is a PEM-encoded client key (or a
                            path on the node).
                          type: string
                        insecure:
                          description: Insecure skips TLS verification.
                          type: boolean
                        url:
                          description: URL of the probe target.
                          minLength: 1
                          type: string
                      type: object
                    name:
                      description: |-
                        Name identifies the probe and is referenced by Output and by
                        instruction-status conditions.
                      minLength: 1
                      type: string
                    retryStrategy:
                      description: |-
                        RetryStrategy controls evaluation cadence and the success/failure
                        thresholds that flip the probe's reported status.
                      properties:
                        failureThreshold:
                          description: |-
                            FailureThreshold is the consecutive failures required to flip the
                            probe to NotReady.
                          type: integer
                        initialDelaySeconds:
                          description: InitialDelaySeconds delays the first evaluation.
                          type: integer
                        successThreshold:
                          description: |-
                            SuccessThreshold is the consecutive successes required to flip
                            the probe to Ready.
                          type: integer
                        timeoutSeconds:
                          description: TimeoutSeconds bounds a single probe attempt.
                          type: integer
                      type: object
                    selector:
                      description: Selector selects which nodes evaluate the probe.
                      items:
                        description: |-
                          A label selector is a label query over a set of resources. The result of matchLabels and
                          matchExpressions are ANDed. An empty label selector matches all objects. A null
                          label selector matches no objects.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                      type: array
                  type: object
                type: array
              templateRef:
                description: |-
                  TemplateRef references the source ClusterPlanTemplate (cluster-scoped).
                  Empty means the plan was authored directly without a template.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - clusterRef
            - lifecycle
            - operation
            type: object
          status:
            description: ClusterPlanStatus is the observed state of a ClusterPlan.
            properties:
              completedAt:
                description: CompletedAt is when the plan reached a terminal phase.
                format: date-time
                type: string
              conditions:
                description: |-
                  Conditions surface fine-grained progress: BeaconHeld, Rendered,
                  NodePlansDelivered, InstructionsApplied, PatchesApplied,
                  Cancelled, TimedOut.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentStep:
                description: |-
                  CurrentStep is the zero-based index of the pool currently in
                  progress within Spec.Plan.
                type: integer
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last reconciled.
                format: int64
                type: integer
              persistentOutputs:
                additionalProperties:
                  type: string
                description: |-
                  PersistentOutputs holds outputs marked Persistent=true on any
                  NodePlan descended from this ClusterPlan, lifted to the parent
                  for cross-pool access.
                type: object
              phase:
                description: |-
                  Phase is the high-level lifecycle state (see ClusterPlanPhase*
                  constants).
                type: string
              startedAt:
                description: StartedAt is when the Beacon was first requested.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
